name: SetWord
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set Word
        run: chmod +x ./setWord.sh && ./setWord.sh
        working-directory: .
